import {Playfair, Noto_Sans, WindSong} from 'next/font/google'

export const playfair = Playfair({ weight: [], style: [], subsets: ['latin'] })

export const notosans = Noto_Sans({ weight: ['400'], style: ['normal', 'italic'], subsets: ['latin'] })

export const windsong = WindSong({ weight: ['400','500'], style: [], subsets: ['latin'] })
