import {Playfair_Display, Roboto, WindSong} from 'next/font/google'

export const playfair = Playfair_Display({ weight: [], style: [], subsets: ['latin'] })

export const notosans = Roboto({ weight: ['400'], style: ['normal', 'italic'], subsets: ['latin'] })

export const windsong = WindSong({ weight: ['400'], style: [], subsets: ['latin'] })
